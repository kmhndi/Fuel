# WHOOP OAuth credentials from https://developer-dashboard.whoop.com
# Set these as environment variables in your Vercel project (never commit real values).
WHOOP_CLIENT_ID=your_whoop_client_id
WHOOP_CLIENT_SECRET=your_whoop_client_secret
